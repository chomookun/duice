task build(type:GradleBuild) {
    tasks = ['startHttpServer', 'buildTsc']
}

task startHttpServer(type:Exec) {
    commandLine 'npx', 'http-server'
}

task buildTsc(type:Exec) {
    commandLine 'node_modules/typescript/bin/tsc','--watch'
}

